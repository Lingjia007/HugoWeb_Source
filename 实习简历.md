# 实习简历

## 项目经历

### 项目一：STM32F407 安全 Bootloader 设计与实现

基于 STM32F407VGT6 设计工业级安全 Bootloader，解决固件升级过程中断电变砖、固件篡改和版本回滚问题。

- 设计 A/B 乒乓分区机制，升级失败自动回滚，启动尝试超限 3 次触发强制回滚，确保设备永不变砖
- 构建 HMAC-SHA256 → 防回滚 → HKDF 设备绑定 → AES-256 解密 → Ed25519 验证 → SHA-256 回读校验的完整信任链，任何一环失败即中止
- 实现 HKDF（DevKey + UID + Salt）设备绑定密钥派生，固件包只能由目标设备解密
- 设计 Transport 平台抽象层，使下载逻辑与存储介质（UART/SD卡/SPI Flash/WiFi OTA）完全解耦
- 集成 HPatchLite + tuz 差分升级，384KB 固件差分包压缩至 5-20KB，适配带宽受限场景

**技术栈**：C / STM32 HAL / AES-256 / Ed25519 / HKDF / HPatchLite / MQTT / FatFS / LittleFS / OneNET OTA

***

### 项目二：PyIAPToolKit — STM32 IAP 上位机工具套件

为安全 Bootloader 开发配套 PC 端工具，实现固件打包、加密签名到传输烧录的完整工作流。

- 实现固件打包流水线：SHA-256 追加 → HKDF 密钥派生 → AES-256 加密 → Ed25519 签名，一站式输出安全固件包
- 开发 VT100 终端 + YMODEM 传输 + PyOCD SWD 烧录双通道，覆盖开发与生产场景
- 集成 bsdiff4 / HPatchLite 双差分引擎，支持生成端与设备端兼容的 OTA 差分包
- 开发支付宝沙箱SDK上位机模块，通过自定义串口帧协议与 STM32 通信，完成支付闭环测试
- 设计 QThread + pyqtSignal 异步架构，耗时操作子线程执行，主线程 UI 无阻塞

**技术栈**：Python / PyQt6 / pycryptodome / pyserial / pyocd / Nuitka

***

### 项目三：智能售货终端 — LVGL 动态 UI 架构与支付集成

基于 ArtInChip SDK + LVGL v9 + RT-Thread 的智能售货机终端，构建支持动态加载和热更新的嵌入式 GUI 应用。

- 设计"应用树"架构（layout\_node + lv\_obj\_t 双树映射），解决原生 LVGL 动态组件生命周期分散问题
- 实现 Wrap 桥接机制，将 AiUIBuilder 静态容器纳入应用树管理，保留原型能力同时获得运行时灵活性
- 通过 event\_bus 发布-订阅实现商品卡片、购物车、支付模块间零依赖通信
- 设计线程安全方案：业务线程通过 SPSC 环形队列传递数据，LVGL 定时器在自身线程安全处理 UI
- 集成支付宝沙箱双支付模式（用户扫码 + 付款码支付），通过自定义串口帧协议与上位机通信完成支付闭环

**技术栈**：C / RT-Thread / LVGL v9 / Mongoose / quirc / DVP

***

## 技能清单

| 类别    | 技能                                                    |
| ----- | ----------------------------------------------------- |
| 编程语言  | C、Python                                              |
| 嵌入式   | STM32F4、RT-Thread、LVGL、HAL 库、UART/SPI/I2C/SDIO/DVP 驱动 |
| 安全与加密 | AES-256、Ed25519、HKDF、HMAC-SHA256                      |
| 通信协议  | MQTT、YMODEM、UART 自定义协议、HTTP RESTful                   |
| 工具与框架 | PyQt6、Keil MDK、PyOCD、Git                              |

